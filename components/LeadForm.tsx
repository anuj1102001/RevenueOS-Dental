'use client';
import {FormEvent,useState} from 'react';
export default function LeadForm(){const [done,setDone]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});setDone(true)}if(done)return <div className="card"><h3>Consultation requested.</h3><p className="muted">The demo lead has been captured by RevenueOS.</p></div>;return <form className="card" onSubmit={submit}><h3>Request a consultation</h3><p><input name="name" required placeholder="Full name" style={{width:'100%',padding:14,borderRadius:12,border:'1px solid #ddd'}}/></p><p><input name="phone" required placeholder="Phone / WhatsApp" style={{width:'100%',padding:14,borderRadius:12,border:'1px solid #ddd'}}/></p><p><select name="treatment" style={{width:'100%',padding:14,borderRadius:12,border:'1px solid #ddd'}}><option>Smile makeover</option><option>Dental implants</option><option>Clear aligners</option><option>Teeth whitening</option><option>General consultation</option></select></p><button className="button">Request consultation</button></form>}